<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.vadimbliashuk.secondsaveyourpassword">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ChangePinCode"
            android:label="Change Pin code"
            android:parentActivityName=".activities.SettingsActivity"
            android:theme="@style/AppTheme.YesActionBar" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/Settings"
            android:parentActivityName=".activities.AllLoginsActivity"
            android:theme="@style/AppTheme.YesActionBar" />
        <activity
            android:name=".activities.AddNewLogin"
            android:label="Add New Login"
            android:parentActivityName=".activities.AllLoginsActivity"
            android:theme="@style/AppTheme.YesActionBar"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.AllLoginsActivity"
            android:label="All Items" />
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>